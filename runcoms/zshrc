#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Customize to your needs...

# Base16 Shell
BASE16_SHELL="$HOME/.config/base16-shell/base16-eighties.dark.sh"
[[ -s $BASE16_SHELL ]] && . $BASE16_SHELL

# nvm
NVM_SCRIPT="$(brew --prefix nvm)/nvm.sh"
if [[ -f $NVM_SCRIPT ]]; then
	source $NVM_SCRIPT
fi

# virtualenvwrapper
[[ -z $VIRTUAL_ENV ]] && workon default

# chruby
# Must come after virtualenvwrapper so we can prepend .bin to our path
CHRUBY_SCRIPT="$(brew --prefix chruby)/share/chruby/chruby.sh"
if [[ -f $CHRUBY_SCRIPT ]]; then
	source $CHRUBY_SCRIPT
	chruby ruby-2

	# add local bin for binstubs
	path=(
		./bin
		$path
	)
fi

# aliases
ALIASES_FILE="$HOME/.aliases"
[[ -f  $ALIASES_FILE ]] && source $ALIASES_FILE

# functions
FUNCTIONS_FILE="$HOME/.functions"
[[ -f  $FUNCTIONS_FILE ]] && source $FUNCTIONS_FILE
